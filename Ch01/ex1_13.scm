;; ex1_13.scm
;; Kevin C. Baird
;; SICP
;; Exercise 1.13 - Prove that fib(n) is the closest integer to 
;; (phi ** n) / sqrt(5). Hint: Let psi = (1 - sqrt(5))/2. Use 
;; induction and the definition of Fibonacci numbers (see section 
;; 1.2.2) to prove that fib(n) = ((phi ** n) - (psi ** n)) / sqrt(5).

; Look at page 38 for phi and psi
